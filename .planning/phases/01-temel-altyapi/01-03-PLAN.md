---
phase: 01-temel-altyapi
plan: 03
type: execute
---

<objective>
Temel Agent base class ve çalışan ilk agent örneği.

Purpose: Tüm Sade agent'larının miras alacağı base class oluşturmak ve sistemin çalıştığını doğrulamak.
Output: SadeAgent base class, örnek test agent, çalışan ilk agent çıktısı.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-temel-altyapi/01-01-SUMMARY.md
@.planning/phases/01-temel-altyapi/01-02-SUMMARY.md
@architecture/agent_system_design.md

**Önceki planlar:** Proje iskeleti ve CrewAI kurulu.

**Agent tasarım prensipleri (agent_system_design.md'den):**
- "Connoisseur Chip" - domain expertise
- Mixed autonomy model
- Her agent'ın belirli persona'sı var
- Narrator tüm çıktıları "Sade sesi" ile filtreler
</context>

<tasks>

<task type="auto">
  <name>Görev 1: SadeAgent base class</name>
  <files>src/sade_agents/agents/base.py</files>
  <action>
CrewAI Agent'ını extend eden SadeAgent base class oluştur:

```python
from crewai import Agent

class SadeAgent(Agent):
    """
    Tüm Sade Chocolate agent'larının base class'ı.

    "Connoisseur Chip" felsefesi: Sadece kod değil,
    çikolata craft'ını bilen dijital şef ruhu.
    """
```

Base class özellikleri:
- brand_voice: str = "sessiz_luks" (default)
- department: str (finance, marketing, operations, product)
- autonomy_level: str = "mixed" (autonomous, supervised, mixed)
- Ortak logging/error handling

NOT: Şimdilik basit tut. Narrator filtresi Phase 2'de eklenecek.
  </action>
  <verify>python -c "from sade_agents.agents.base import SadeAgent; print('SadeAgent OK')"</verify>
  <done>SadeAgent class import edilebilir, temel özellikler tanımlı</done>
</task>

<task type="auto">
  <name>Görev 2: Test agent ve basit görev</name>
  <files>src/sade_agents/agents/test_agent.py, scripts/run_test_agent.py</files>
  <action>
1. `test_agent.py` - Basit bir test agent:
- SadeAgent'tan miras alır
- role: "Test Agent"
- goal: "Sade Chocolate sisteminin çalıştığını doğrulamak"
- backstory: Kısa Türkçe açıklama

2. `scripts/run_test_agent.py` - Çalıştırma scripti:
- Settings'ten config yükle
- Test agent oluştur
- Basit bir görev çalıştır: "Merhaba, ben çalışıyorum" tarzı
- Çıktıyı ekrana bas

NOT: API key olmadan çalışmayacak. Script başında "OPENAI_API_KEY gerekli" uyarısı ekle.
  </action>
  <verify>python scripts/run_test_agent.py --dry-run (API çağırmadan syntax kontrolü)</verify>
  <done>Test agent kodu yazıldı, syntax hatasız</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Temel agent altyapısı ve test agent</what-built>
  <how-to-verify>
    1. .env dosyası oluştur: `cp .env.example .env`
    2. OPENAI_API_KEY'i ekle
    3. Çalıştır: `python scripts/run_test_agent.py`
    4. Agent'ın yanıt ürettiğini doğrula
    5. Hata yoksa sistem hazır
  </how-to-verify>
  <resume-signal>"onaylandı" yazarak devam et veya sorunları bildir</resume-signal>
</task>

</tasks>

<verification>
Plan tamamlanmadan önce:
- [ ] SadeAgent class mevcut ve import edilebilir
- [ ] Test agent tanımlı
- [ ] Run script mevcut
- [ ] Kullanıcı test çalıştırdı ve onayladı
</verification>

<success_criteria>
- Tüm görevler tamamlandı
- Kullanıcı agent çalışmasını onayladı
- Phase 1: Temel Altyapı tamamlandı
</success_criteria>

<output>
Tamamlandığında `.planning/phases/01-temel-altyapi/01-03-SUMMARY.md` oluştur.

Son plan olduğu için ayrıca:
- STATE.md güncelle (Phase 1 complete)
- ROADMAP.md progress tablosunu güncelle
</output>
