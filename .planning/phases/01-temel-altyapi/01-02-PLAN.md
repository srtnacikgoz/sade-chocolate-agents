---
phase: 01-temel-altyapi
plan: 02
type: execute
---

<objective>
CrewAI framework kurulumu ve konfigürasyon altyapısı.

Purpose: Agent'ların çalışması için gerekli bağımlılıkları kurmak ve çevre değişkeni yönetimini hazırlamak.
Output: Kurulu CrewAI, .env template ve settings modülü.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-temel-altyapi/01-01-SUMMARY.md

**Önceki plan çıktısı:** Proje iskeleti hazır, pyproject.toml mevcut.

**Teknik kararlar:**
- CrewAI ana framework
- python-dotenv ile .env yönetimi
- pydantic-settings ile type-safe config
</context>

<tasks>

<task type="auto">
  <name>Görev 1: Bağımlılıkları pyproject.toml'a ekle ve kur</name>
  <files>pyproject.toml</files>
  <action>
pyproject.toml'a dependencies ekle:
- crewai (ana framework)
- python-dotenv (env yönetimi)
- pydantic-settings (config validation)

Sonra kur:
```bash
pip install -e ".[dev]"
```

NOT: crewai[tools] kullanma - temel kurulum yeterli. Ekstra tool'lar sonra eklenecek.
  </action>
  <verify>python -c "import crewai; print(crewai.__version__)" version çıktısı verir</verify>
  <done>CrewAI ve bağımlılıklar kurulu, import çalışıyor</done>
</task>

<task type="auto">
  <name>Görev 2: Konfigürasyon yönetimi</name>
  <files>src/sade_agents/config/settings.py, .env.example, .gitignore</files>
  <action>
1. `.env.example` oluştur (gerçek değerler olmadan):
```
# OpenAI API (CrewAI için gerekli)
OPENAI_API_KEY=your-api-key-here

# Opsiyonel: Model seçimi
OPENAI_MODEL_NAME=gpt-4o-mini
```

2. `src/sade_agents/config/settings.py` oluştur:
- pydantic-settings ile Settings class
- OPENAI_API_KEY (required)
- OPENAI_MODEL_NAME (default: gpt-4o-mini)
- Validation ile eksik key uyarısı

3. `.gitignore` güncelle (yoksa oluştur):
- .env
- __pycache__/
- *.pyc
- .venv/
- dist/
- *.egg-info/

NOT: .env dosyasının kendisini oluşturma - sadece .env.example. Kullanıcı kendi API key'ini ekleyecek.
  </action>
  <verify>python -c "from sade_agents.config.settings import Settings; print('Settings class OK')"</verify>
  <done>Settings class çalışıyor, .env.example mevcut, .gitignore güncel</done>
</task>

</tasks>

<verification>
Plan tamamlanmadan önce:
- [ ] `pip list | grep crewai` çıktı veriyor
- [ ] Settings class import edilebiliyor
- [ ] .env.example mevcut
- [ ] .gitignore .env'i içeriyor
</verification>

<success_criteria>
- Tüm görevler tamamlandı
- CrewAI kurulu ve import edilebilir
- Config altyapısı hazır
</success_criteria>

<output>
Tamamlandığında `.planning/phases/01-temel-altyapi/01-02-SUMMARY.md` oluştur.
</output>
