---
phase: 01-temel-altyapi
plan: 01
type: execute
---

<objective>
Python proje iskeleti ve dizin yapısını oluşturmak.

Purpose: Tüm agent'ların üzerine inşa edileceği temel proje yapısını kurmak.
Output: Çalışan Python projesi ile standart src layout ve pyproject.toml.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@architecture/agent_system_design.md
@skills/README.md

**Mevcut durum:** Proje şu an sadece dokümantasyon içeriyor. Python kodu yok.

**Kararlar:**
- Python + CrewAI stack (PROJECT.md Key Decisions)
- Skills as Python functions (modüler yapı)
- src layout kullanılacak (modern Python standardı)
</context>

<tasks>

<task type="auto">
  <name>Görev 1: pyproject.toml ve proje meta verisi</name>
  <files>pyproject.toml</files>
  <action>
Modern Python projesi için pyproject.toml oluştur:
- name: "sade-agents"
- version: "0.1.0"
- python: ">=3.11"
- Henüz bağımlılık ekleme (sonraki planda)
- src layout için packages yapılandırması
- Temel metadata (description, authors)

NOT: setup.py kullanma - modern Python pyproject.toml kullanır.
  </action>
  <verify>python -c "import tomllib; tomllib.load(open('pyproject.toml', 'rb'))" hatasız çalışır</verify>
  <done>Geçerli pyproject.toml mevcut, TOML syntax doğru</done>
</task>

<task type="auto">
  <name>Görev 2: Dizin yapısı ve modül dosyaları</name>
  <files>src/sade_agents/__init__.py, src/sade_agents/agents/__init__.py, src/sade_agents/skills/__init__.py, src/sade_agents/config/__init__.py, tests/__init__.py</files>
  <action>
Aşağıdaki dizin yapısını oluştur:

```
src/
  sade_agents/
    __init__.py          # version ve temel export
    agents/
      __init__.py        # agent base class export edilecek
    skills/
      __init__.py        # skill base class export edilecek
    config/
      __init__.py        # config yönetimi
tests/
  __init__.py
```

Her __init__.py dosyasına:
- Modül docstring (Türkçe açıklama)
- `__all__` listesi (şimdilik boş)

NOT: Boş dosya oluşturma - her dosyada en az docstring olsun.
  </action>
  <verify>python -c "import sys; sys.path.insert(0, 'src'); import sade_agents" hatasız çalışır</verify>
  <done>Tüm dizinler ve __init__.py dosyaları mevcut, import çalışıyor</done>
</task>

</tasks>

<verification>
Plan tamamlanmadan önce:
- [ ] pyproject.toml geçerli TOML formatında
- [ ] src/sade_agents/ dizini ve alt modüller mevcut
- [ ] tests/ dizini mevcut
- [ ] Temel import çalışıyor
</verification>

<success_criteria>
- Tüm görevler tamamlandı
- Doğrulama kontrolleri geçti
- Python proje yapısı standartlara uygun
</success_criteria>

<output>
Tamamlandığında `.planning/phases/01-temel-altyapi/01-01-SUMMARY.md` oluştur.
</output>
