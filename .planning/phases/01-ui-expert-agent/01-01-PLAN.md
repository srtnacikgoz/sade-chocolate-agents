---
phase: 01-ui-expert-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sade_agents/web/components/reference/__init__.py
  - src/sade_agents/web/components/reference/card.py
  - src/sade_agents/web/components/reference/form.py
  - src/sade_agents/web/components/reference/data_table.py
autonomous: true

must_haves:
  truths:
    - "Reference component library 3+ ornek icerir"
    - "Her ornek type hints, docstring ve ornek kullanim icerir"
    - "Ornekler Sade Chocolate styling conventions'a uygun"
  artifacts:
    - path: "src/sade_agents/web/components/reference/card.py"
      provides: "Card component ornegi"
      min_lines: 40
    - path: "src/sade_agents/web/components/reference/form.py"
      provides: "Form component ornegi"
      min_lines: 50
    - path: "src/sade_agents/web/components/reference/data_table.py"
      provides: "Data table component ornegi"
      min_lines: 40
  key_links:
    - from: "src/sade_agents/web/components/reference/__init__.py"
      to: "card.py, form.py, data_table.py"
      via: "module exports"
      pattern: "from.*import"
---

<objective>
Reference Component Library olusturmak - AI code generation icin "golden examples".

Purpose: AI hallucination'i minimize etmek icin production-ready Streamlit ornekleri saglamak.
Arastirmaya gore reference library %95'ten %15'e hata oranini dusurur (6x iyilestirme).

Output: 3 adet production-ready Streamlit component ornegi (card, form, data_table)
</objective>

<execution_context>
@C:\Users\Hp\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Hp\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-ui-expert-agent/01-RESEARCH.md

# Mevcut component ornekleri (pattern icin)
@src/sade_agents/web/components/result_viewer.py
@src/sade_agents/web/components/sidebar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card Component Reference</name>
  <files>src/sade_agents/web/components/reference/card.py</files>
  <action>
Production-ready card component ornegi olustur:

1. Docstring: Bu dosyanin AI reference icin oldugunu acikla
2. Imports: streamlit, typing (Optional, Literal)
3. render_card fonksiyonu:
   - title: str
   - content: str
   - icon: Optional[str] = None
   - variant: Literal["default", "primary", "warning"] = "default"
   - Return type: None
4. Streamlit layout: st.container, st.columns (1:20 ratio), st.markdown
5. Google-style docstring: Args, Example
6. En altta "Usage example for AI training" comment blogu

Sade Chocolate styling:
- Emoji icons yerine minimal iconlar
- st.markdown ile clean typography
- Container-based layout (no absolute positioning)

KACINILACAKLAR:
- Hardcoded hex colors (CSS variables kullan veya Streamlit default)
- st.beta_* veya st.experimental_* (deprecated)
- Complex nested structures
  </action>
  <verify>python -c "from src.sade_agents.web.components.reference.card import render_card; print('OK')"</verify>
  <done>render_card fonksiyonu import edilebilir, type hints ve docstring mevcut</done>
</task>

<task type="auto">
  <name>Task 2: Form Component Reference</name>
  <files>src/sade_agents/web/components/reference/form.py</files>
  <action>
Production-ready form component ornegi olustur:

1. Docstring: AI reference oldugunu belirt
2. Imports: streamlit, typing, Optional, Literal, Callable
3. render_form fonksiyonu:
   - title: str
   - fields: list[dict] (her dict: name, type, label, required, placeholder)
   - on_submit: Optional[Callable] = None
   - submit_label: str = "Gonder"
   - Return type: dict | None (form data veya None)
4. st.form context manager kullan
5. Field types: text_input, number_input, selectbox, text_area
6. Validation: required fields kontrolu
7. Google-style docstring

Field dict yapisi ornegi:
```python
fields = [
    {"name": "product_name", "type": "text", "label": "Urun Adi", "required": True, "placeholder": "Orn: Ruby Truff"},
    {"name": "price", "type": "number", "label": "Fiyat (TL)", "required": True},
    {"name": "category", "type": "select", "label": "Kategori", "options": ["Kutu", "Tablet", "Hediye"]},
]
```

KACINILACAKLAR:
- Global state (session_state sadece form icinde)
- Magic numbers (field limitleri icin constants)
  </action>
  <verify>python -c "from src.sade_agents.web.components.reference.form import render_form; print('OK')"</verify>
  <done>render_form fonksiyonu import edilebilir, field dict yapisi destekleniyor</done>
</task>

<task type="auto">
  <name>Task 3: Data Table Component Reference + Module Init</name>
  <files>
    src/sade_agents/web/components/reference/data_table.py
    src/sade_agents/web/components/reference/__init__.py
  </files>
  <action>
1. data_table.py - Production-ready table component:

Docstring: AI reference
Imports: streamlit, pandas, typing (Literal)

render_data_table fonksiyonu:
- data: pd.DataFrame
- title: str
- variant: Literal["default", "compact", "striped"] = "default"
- show_download: bool = True
- Return type: None

Features:
- Header: st.markdown ile title
- Table: st.dataframe (compact/striped) veya st.table (default)
- Download: st.download_button ile CSV export
- Google-style docstring + usage example

2. __init__.py - Module exports:

```python
"""
Reference Component Library for AI Code Generation.

Bu moduldeki ornekler, UI Expert Agent'in Streamlit kod uretirken
referans olarak kullandigi "golden examples" dir.

Kurallar:
- Her component type hints icerir
- Her component Google-style docstring icerir
- Her component en altta usage example icerir
- Hardcoded degerler yerine parametreler kullanilir
"""

from sade_agents.web.components.reference.card import render_card
from sade_agents.web.components.reference.form import render_form
from sade_agents.web.components.reference.data_table import render_data_table

__all__ = ["render_card", "render_form", "render_data_table"]
```
  </action>
  <verify>python -c "from src.sade_agents.web.components.reference import render_card, render_form, render_data_table; print('All exports OK')"</verify>
  <done>3 reference component export ediliyor, her biri type hints + docstring icerir</done>
</task>

</tasks>

<verification>
```bash
# Syntax check
python -m py_compile src/sade_agents/web/components/reference/card.py
python -m py_compile src/sade_agents/web/components/reference/form.py
python -m py_compile src/sade_agents/web/components/reference/data_table.py

# Import check
python -c "from src.sade_agents.web.components.reference import render_card, render_form, render_data_table; print('Reference library ready')"

# Type hints check (mypy varsa)
# mypy src/sade_agents/web/components/reference/
```
</verification>

<success_criteria>
- 3 reference component dosyasi mevcut
- Her dosya import edilebilir (syntax valid)
- Her fonksiyon type hints icerir
- Her fonksiyon Google-style docstring icerir
- Her dosya en altta usage example icerir
- __init__.py tum component'lari export eder
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-expert-agent/01-01-SUMMARY.md`
</output>
